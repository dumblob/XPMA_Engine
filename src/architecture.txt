routine place_gui_item(
  _type: list<scalar_db_type>|scalar_db_type,
  # anticipated number of these items in the DB (relative to all other
  #   items in the same DB schema)
  count: float,
  # anticipated importance of this item relative to all other items in
  #   the same DB schema
  importance: float,
  relations: list<gui_item> ){
    std.assert(amount     >= 0 and amount     <= 1)
    std.assert(importance >= 0 and importance <= 1)
}
